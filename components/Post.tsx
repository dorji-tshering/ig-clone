

const Post = ({ id, username, avatar, image, caption }: {id: string, username: string, avatar: string, image: string, caption: string}) => {
    return (
        <div>
            
        </div>
    )
}

export default Post;